---
extends:
  - "@commitlint/config-conventional"
rules:
  header-max-length:
    - 2
    - always
    - 96
  body-max-line-length:
    - 0
    - always
  footer-max-line-length:
    - 0
    - always
  type-enum:
    - 2
    - always
    - - build
      - ui
      - ci
      - docs
      - feat
      - fix
      - perf
      - refactor
      - revert
      - format
      - test
      - chore
